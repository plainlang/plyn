; Bracket matching for the ***plain language. Tree-sitter-plain treats
; template expressions, include directives, and markdown links as single
; atomic tokens, so there are no nested bracket pairs to surface here.
; This file is kept intentionally minimal but present so editors that look
; for it (e.g. Zed) don't warn.
